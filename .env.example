# Local demo mode requires no secret and should only be exposed on a trusted machine.
PLATFORM_AUTH_MODE=demo
PLATFORM_AUTH_ROLE=platform_admin
PLATFORM_BIND_ADDRESS=127.0.0.1

# To protect a shared instance, switch to token mode and use a generated secret
# containing at least 16 characters. Do not commit the real value.
PLATFORM_API_TOKEN=
